# 续页隐私说明（本机测试版）

续页当前版本是本机优先应用。它不连接互联网或云服务；只有在用户主动开启“从手机接收”时，才会临时接受同一局域网内设备的入站连接。用户明确启用数据备份后，App 还可以把加密恢复点写入用户选择的文件夹；文件夹是否由第三方网盘客户端上传，不由续页控制或观察。

- 用户选择或从手机上传的报告会被复制到 App Sandbox 内的本机资料库；Mac 上原先选择的源文件不会被修改或删除。
- 用户选择的 DICOM 文件夹会被复制为 App 管理的明文原件和索引；源文件夹不会被修改。App 要求用户确认家庭成员和检查日期，确认后只把检查日期、成员和对象数量等检查级摘要放入对应成员的时间线；DICOM 原件、自由文本和像素不进入报告 OCR、搜索或比较。受支持的二维 MR 切片只在按需查看时进入有界内存；App 不为像素执行 OCR、搜索或自动导出，也不创建持久预览或截图。
- 活动资料库不进行应用层加密，也不使用 Keychain。报告原件、成员资料、OCR 结果和搜索字段以可读取的本地文件保存；只有用户启用后写入外部目录的 `.kinloguebackup` 恢复点使用应用层认证加密。
- SHA-256 摘要用于发现意外损坏和本机精确去重，不能隐藏内容，也不能抵御能够重写资料库的攻击者。
- OCR 使用 macOS Vision 在本机执行；App 不把报告或识别结果发送到互联网、云服务或第三方。
- App 不包含账号、互联网客户端、内置云同步、iCloud、CloudKit、HealthKit、广告、遥测或第三方崩溃 SDK。
- 局域网接收由用户明确开启，使用普通 HTTP，不提供传输加密，只应在可信任的私人 Wi-Fi 或有线网络使用。地址和一次性验证码仅用于当前接收会话。
- 手机页面只能查看当前会话的上传状态，不能浏览 Mac 上的家庭成员、历史报告、OCR 内容或时间线。
- 主动停止、切换网络、锁定或睡眠 Mac、关闭最后一个主窗口或退出 App 会停止接收；唤醒、重开窗口或重新启动 App 不会自动恢复。
- 只有用户确认过的来源转录才进入时间线、搜索和比较；App 不生成诊断、趋势或治疗建议。
- 数据备份由用户明确选择父目录，续页只在固定专用子目录中写入经过完整回读验证的加密恢复点。自动备份默认关闭；默认保留 5 份，可设为 2–30 份。这个数量只描述本地目录，不能证明远端网盘也保留相同份数。
- 恢复码由用户独立保管；恢复私钥不写入 Keychain、本机配置或备份目录。遗失恢复码时，续页无法替用户找回或解密既有恢复点。
- 首次备份设置中断后，用户可以输入自己保存的原恢复码继续同一备份身份，或经二次确认放弃本机未完成设置；该输入不会写入配置、日志或 repository，并在成功、失败、取消或放弃后从当前界面状态清空。
- 恢复会先在 App 私有目录解密并完整验证，只有用户再次确认后才替换而不是合并当前资料库；成功后必须重新启动。外部恢复点不会因恢复或删除本机资料库而自动删除。
- 用户可以在“设置 → 数据管理”明确导出一个明文 ZIP。它只包含已确认报告和已确认 DICOM 检查的原始文件，按家庭成员和日期排列；不包含 OCR、转录、备注、索引、草稿、待确认 DICOM 或手机待确认项。该 ZIP 是供医生查看或打印的副本，不是资料库备份，不能用于恢复续页。
- 手机上传完成的唯一原件会保留在本机待确认队列，直到用户明确删除或成功归档所选项。相同原件会按完整内容合并；归档失败时队列项仍会保留。报告加入等待确认后，删除投递箱中的接收副本不会删除已归档原件。
- 删除操作只影响 App 管理的本机资料库和本机备份设置，不会删除 Mac 上最初选择的源文件，也无法擦除已导出的 ZIP、已发布的加密恢复点、Time Machine 备份、APFS 快照或其他外部副本。
- 检测到旧版加密资料库时，App 不会自动迁移、覆盖或删除它。

真实病历只允许从仓库外位置进行私密人工验收，不得进入 Git、日志、自动化测试、截图或分发产物。公开源码前还必须检查所有准备公开的 Git reachable history；仅从当前工作树删除敏感内容并不会将它从旧 commit 中移除。
